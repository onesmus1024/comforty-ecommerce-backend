
let config = {
    server : 'localhost',
    authentication: {
        type: 'default',
        options: {
            userName: 'sa',
            password: 'on07mawa'
        }
    },
    options: {
        database: 'ecommerce',
        encrypt: true,
        trustServerCertificate: true,
        
    }
};

export default config;